[uwsgi]

chdir          = /srv/www/{{ domain_name }}/code

module         = {{ app_name }}.wsgi

home           = /srv/web/{{ domain_name }}/venv

master         = true

processes      = {{ process_number }}

socket         = /srv/www/{{ domain_name }}/uwsgi/{{ app_name }}.sock

vacuum         = true
